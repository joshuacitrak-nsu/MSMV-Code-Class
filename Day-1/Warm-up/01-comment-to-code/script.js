// TODO: Get the score display element and the button element from the page.
const scoreDisplay = document.getElementById("score");
const clickButton = document.getElementById("clickButton");

// We will keep track of the score in this variable.
let score = 0;

// TODO: Write the addScore() function.
// It should: (1) increase score by 1, (2) update the scoreDisplay to "Score: X".
function addScore() {
  // your code here
}

// TODO: When the button is clicked, run addScore().
clickButton.addEventListener("click", addScore);
